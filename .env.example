# Sinhala ASR Dataset Collection Service - Environment Configuration

# =============================================================================
# DATABASE CONFIGURATION (Required)
# =============================================================================
# PostgreSQL connection string
# Format: postgresql://username:password@hostname:port/database_name
# For Supabase: Get from Project Settings > Database > Connection string > URI
DATABASE_URL=postgresql://username:password@localhost:5432/sinhala_asr_db

# =============================================================================
# GOOGLE CLOUD STORAGE CONFIGURATION (Required)
# =============================================================================
# Name of the Google Cloud Storage bucket containing audio files
# Create a bucket with a globally unique name in Google Cloud Console
GCS_BUCKET_NAME=sinhala-asr-audio-dataset-2025

# =============================================================================
# AUTHENTICATION CONFIGURATION (Optional)
# =============================================================================
# Base64 encoded Google Cloud service account JSON
# If not provided, the application will use Application Default Credentials (ADC)
# To generate: base64 -i /path/to/service-account.json (Linux/Mac)
# To generate: [Convert]::ToBase64String([IO.File]::ReadAllBytes("path\to\service-account.json")) (Windows PowerShell)
SERVICE_ACCOUNT_B64=

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Enable debug mode for detailed logging
DEBUG=true